# Distribution
# TODO @paipo: It's just a quick around for now,
# should be commented back and add the build pipeline on CI in the future.
# build/

# elm-package generated files
elm-stuff

# elm-repl generated files
repl-temp-*

# Dependency directories
node_modules

# Desktop Services Store on macOS
.DS_Store
